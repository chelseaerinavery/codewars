//7kyu: Remove the minimum

function removeSmallest(numbers) {
    const copy = [...numbers]
    let minPosition = numbers.indexOf(Math.min(...numbers))
    copy.splice(minPosition, 1);
    return copy
}
